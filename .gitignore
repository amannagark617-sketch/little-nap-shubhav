# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment — never commit real keys
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Intermediate artwork extracted from the corporate deck (~27 MB of PNGs).
# The optimised WebP output in public/images is committed instead — that is the
# source of truth for the site. See scripts/README.md to regenerate.
scripts/_source/
